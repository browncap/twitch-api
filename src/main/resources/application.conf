com.twitch {

  http {
    port = 8081
    port = ${?PORT}
    host = "0.0.0.0"
  }

  twitch {
    client-id = "client-id"
    client-id = ${?CLIENT_ID}
    client-secret = "client-secret"
    client-secret = ${?CLIENT_SECRET}
  }

}
